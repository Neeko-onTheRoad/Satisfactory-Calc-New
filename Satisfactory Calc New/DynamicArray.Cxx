#include "DynamicArray.Hxx"

template<class T>
T* MakeDynamicArray(size_t size, T allocator) {
	T* result = new T[size];
	
	for (int i = 0; i < size; i++) {
		result[i] = allocator;
	}

	return result;
}
